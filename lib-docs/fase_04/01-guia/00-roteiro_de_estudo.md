# Fase 04 - Roteiro

1. Abra `models/cidade.dart`.
2. Abra `dao/cidade_dao.dart`.
3. Veja como a tela chama o DAO em `app/cidade_lista.dart`.
4. Veja como o formulario usa `DropdownButton<Estado>`.

Foco da fase: CRUD de cidade sem SQL direto na tela.
